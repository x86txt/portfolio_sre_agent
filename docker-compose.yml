version: '3.8'

# Note: This docker-compose.yml is for local development only.
# The backend uses SQLite3 for report caching (no separate service needed).
# For production deployment on Fly.io, see README.md
